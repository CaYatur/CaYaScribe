{
  "name": "cayascribe",
  "private": true,
  "version": "0.1.0",
  "description": "Local speaker-aware transcription for Windows",
  "scripts": {
    "desktop": "npm --prefix apps/desktop run tauri --",
    "desktop:dev": "npm --prefix apps/desktop run tauri -- dev",
    "desktop:build": "npm --prefix apps/desktop run tauri -- build"
  }
}
